const config = {
   "preset": "@shelf/jest-mongodb",
  transform: {
    "^.+\\.ts$": "ts-jest",
  },
  // testEnvironment: "node",
  // setupFilesAfterEnv: ["./test/setup.ts"],
};

export default config;
